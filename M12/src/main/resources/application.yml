spring:
  application:
    name: M12
  config:
    import: optional:configserver:http://localhost:5555
server:
  port: 9090
management:
  endpoints:
    web:
      exposure:
        include: refresh
        