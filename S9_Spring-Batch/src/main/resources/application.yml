spring:
  application:
    name: Spring-Batch
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springbatchdb
    password: 1234
    username: root
  jpa:
    hibernate:
       ddl-auto: update
    show-sql: true
  batch:
    job:
      enabled: false
server:
  port: 9090
